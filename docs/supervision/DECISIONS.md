# Decisiones del supervisor externo

| ID | Fecha | Decisión | Motivo | Evidencia | Riesgo | Reversible | Aprobación humana |
|---|---|---|---|---|---|---|---|
| D-000 | 2026-08-03 | Iniciar por auditoría total antes de nuevas capacidades | Evitar construir sobre rutas rotas o simuladas | `CLAUDE.md` | Bajo | Sí | No requerida |
| D-001 | 2026-08-03 | Reutilizar `triade/observability/{contracts,file_graph,neural_graph,export}.py` de la rama `feat/internal-system-graphs` (PR #68) en vez de escribir un generador nuevo | PR #69 exige el script en su arranque y no existía. Escribir otro habría creado dos sistemas de grafos compitiendo, que es justo lo que `CLAUDE.md` prohíbe. Se importaron y se extendieron | F-001; `git log feat/claude-external-supervisor..origin/feat/internal-system-graphs` | Medio: si #68 y #69 se fusionan por separado habrá que resolver el solape en esos cinco ficheros | Sí, revirtiendo el commit | **Requerida antes del merge**: decidir el orden de #68 y #69 |
| D-002 | 2026-08-03 | Excluir `node_modules`, `.git` y el contenido de los directorios de datos (`runs`, `artifacts`, `logs`, `models`, `data`, `.triade_trash`) del atlas físico, contándolos sin expandirlos | Con ellos el atlas tenía 96 036 nodos y 74 665 eran salidas de ejecución: dejaba de servir como mapa. El volumen sigue registrado como metadato, así que no se pierde evidencia | F-012; `file_graph.json` antes y después | Bajo | Sí | No requerida |
| D-003 | 2026-08-03 | Extraer el SQL sólo de literales de cadena del AST, nunca del texto plano del fichero | `FROM (\w+)` sobre texto plano convertía `from pathlib import Path` en una tabla. El grafo de tablas era falso en más de la mitad de sus nodos | F-013; 1 032 → 279 tablas | Bajo | Sí | No requerida |
| D-004 | 2026-08-03 | Corregir la compuerta de `semantic_memory_governance` apuntándola al almacén vivo, y **no** migrar los otros 10 lectores de `semantic_memory` en este ciclo | Es el cambio mínimo que desbloquea la capacidad. Migrar los lectores exige mapear columnas distintas (`key`/`value` frente a `document_id`/`content`) y merece su propio PR con sus pruebas | F-002, F-014; `mission_planner.py:350` | Bajo: una consulta, sin escritura, sin migración de datos | Sí, revirtiendo el commit | No requerida |
| D-005 | 2026-08-03 | Servir los grafos como observabilidad reactiva separando estructura (AST, cacheada 60 s) de señales (SQLite, 0,08 s por pulso) | Recalcular el AST completo cada dos segundos cuesta más que el runtime observado. Lo que cambia en caliente son las filas, no la estructura | `apps/internal_graphs_live.py:build_signals`; medido en 0,08 s | Bajo: sólo lectura, sin escritura | Sí | No requerida |
| D-006 | 2026-08-03 | **Revocada por D-007.** No tocar `neural_graph.py` para arreglar F-003 | La prueba en rojo era de PR #68 y su arreglo cambia cómo se identifican los registros de `neuron_activity` | F-003 | Bajo | — | Corresponde a #68 |
| D-007 | 2026-08-03 | Arreglar F-003 en esta rama: emitir `uses_neuron` entre las entidades que la fila referencia (run → neurona) en vez de sólo desde el nodo del registro | D-006 dejaba la suite de #69 en rojo por un fichero que yo mismo importé: eso es enviar un fallo, no respetarlo. El arreglo es de tres líneas y la relación que restaura no existía en el grafo | `neural_graph.py:_append_relations`; aristas 1 282 → 1 607; suite 1 962/1 962 | Bajo | Sí | No requerida; conviene avisar a #68 |
| D-008 | 2026-08-03 | Aprobar F-018: los errores y gates bajos generan conocimiento, por la ruta gobernada y siempre como `candidate` | Decisión explícita del operador. Un fallo se registraba en `worker_events` y ahí moría, así que el sistema repetía el error. El límite —candidato, no evidencia— evita el riesgo inverso: que un error se vuelva doctrina por haber ocurrido | F-018; `production_injection.PRODUCTION_STATES`; 3 pruebas nuevas | Medio: la cola de aprendizaje crecerá con cada fallo; se acota a 6 estados y se excluye `skipped` | Sí, revirtiendo el commit | **Concedida** por el operador |
| D-009 | 2026-08-03 | Regenerar los grafos **a demanda de quien los lee**, en segundo plano y con publicación atómica, en vez de por temporizador o en caliente | Decisión del operador ante F-033: lo que importa es la continuidad de la acción, no sólo lanzarla. Un escaneo cuesta 53 s medidos: hacerlo en caliente congela la petición, y hacerlo por temporizador quema ese coste aunque nadie tenga la página abierta. Se sirve lo que hay con su edad declarada y se reconstruye detrás; la respuesta siguiente ya trae lo nuevo | F-033; `triade/observability/refresh.py`; medido: 1ª petición 0,399 s con `trigger=started`, 2ª 0,105 s con `trigger=running`, reconstrucción 55,0 s | Bajo: sólo lectura del repo y escritura en `artifacts/`, un hilo daemon, un escaneo simultáneo como máximo | Sí, revirtiendo el commit | No requerida |
| D-010 | 2026-08-03 | Elegir el candidato de evidencia **por medibilidad**, con la misma sonda que usa el handler, y quitar el filtro `source_type='experience'` | F-037: el filtro dejaba 1 elegible de 665 y ese uno no era medible, así que el planner lo reintentaba para siempre. Usar `extract_target` en la selección cuesta una regex sobre contenido ya leído; equivocarse costaba un ciclo entero. El ritmo sigue siendo **un candidato por ciclo**, que es el único freno real de esta rama —el worker no consulta al metabolismo (F-043)—, así que ampliar el filtro no multiplica el gasto de inferencia | F-037; `mission_planner.py:355`; 6 pruebas en `tests/test_evidence_selection_livelock.py` | Bajo: misma cadencia, mejor elección | Sí | No requerida |
| D-011 | 2026-08-03 | Registrar el veredicto de los intentos que no producen comparación (`record_inconclusive`), con una decisión que nunca está en `PROMOTABLE_DECISIONS` | Era la otra mitad del livelock: sin rastro del intento, el `NOT EXISTS` seguía siendo cierto y el mismo candidato volvía a la cola. Se afirma que **no se pudo medir**, que es un hecho distinto de no haberlo intentado, y `require_improvement()` lo rechaza igual. Una prueba impide usar el atajo para declarar `improved` sin experimento | F-037; `evidence_bridge.record_inconclusive`; `test_no_se_puede_declarar_improved_sin_comparacion` | Bajo | Sí | No requerida |
| D-012 | 2026-08-03 | Que el informe de deuda **entre en la planificación**, no sólo en el panel | Petición explícita del operador: el grafo debe servirle de introspección a la propia Tríade. `_plan_system_debt` priorizaba por una proporción entre runs y episodios, un indicio que no sabe nada de módulos sin importador ni de tablas sin lector; y `unexecuted_task_types()`, cuyo docstring dice "atajo para quien quiera actuar sobre la deuda", no lo llamaba nadie salvo un test. Se lee con `allow_build=False` —planificar no puede costar un escaneo del AST— y si no hay grafos se cae al indicio anterior en vez de dar por bueno que no hay deuda | D-009; `mission_planner._debt_snapshot`; verificado sobre copia de la base viva: `source=internal_graphs`, deuda 69 | Bajo: sólo lectura | Sí | No requerida |
| D-013 | 2026-08-03 | Arrancar el watchdog como **hilo del proceso de la API**, no como el servicio de systemd declarado | F-040: el watchdog estaba escrito, probado e inervado, y llevaba 3 días sin ejecutarse porque su lanzador vive en `deploy/systemd/` y systemd no gobierna este Studio —el runtime corre bajo `nohup uvicorn`—. Los workers ya habían resuelto lo mismo así. No sustituye a la unidad: donde systemd mande, que la use. Se arranca una sola vez —dos watchdogs recuperando a la vez es lo que el presupuesto de `RuntimeWatchdog` evita— y un fallo de ciclo nunca mata el hilo, porque un watchdog que muere en silencio promete una vigilancia que ya no da | F-040; `triade/runtime/watchdog_autostart.py`; verificado arrancando la app: `ticks=1`, `state=healthy`, y `runtime_health_snapshots` vuelve a escribir tras 3 días | Bajo: sólo observa; recuperar exige que el estado sea `stalled/critical/stopped` y tiene presupuesto y cooldown | Sí, revirtiendo el commit | No requerida |
| D-014 | 2026-08-03 | Consultar al governor de recursos **en cada ciclo** del worker y bloquear el ciclo si el modo permitido es `blocked` | F-043: el único freno era `check_ollama_blood()`, que mira si el modelo responde, no cuánto se gasta. El governor que decide por RAM, disco, carga, batería y estado térmico ya existía y sólo se consultaba una vez, al arrancar los workers; los recursos cambian durante la sesión. Dos límites deliberados: el lock se suelta siempre en el bloqueo —retenerlo cambiaría una parada temporal por un bloqueo permanente— y un sensor roto devuelve `unknown` y deja pasar, porque frenar por no saber sería cambiar una parada de recursos por una de sensor | F-043; `worker_loop._governor_decision`; 8 pruebas en `tests/test_resource_gate_and_watchdog.py` | Medio: un umbral mal calibrado pararía trabajo legítimo; por eso sólo bloquea en `blocked`, no en modos degradados | Sí, revirtiendo el commit | No requerida |
| D-015 | 2026-08-03 | Convertir la ausencia de backup en **deuda medida** (`backup_protection_gaps`) en vez de en silencio | F-046: el sistema pasó cuatro días sin copia y ninguna señal cambió, porque el fallo era que algo dejara de ocurrir. Se miden tres faltas distintas porque fallan por separado: sin clave, copia más vieja que la cadencia declarada (`OnCalendar=daily`, con dos días de margen), y copia cuyo manifiesto no identifica la clave. La categoría puede llegar a cero, condición para que sea alarma y no decorado | F-046, F-047; `introspection._backup_protection_gaps`; 10 pruebas | Bajo: sólo lectura de `artifacts/backups` y del entorno | Sí | No requerida |
| D-016 | 2026-08-03 | Escribir `key_fingerprint` en el manifiesto de cada copia nueva | F-047: sin identificar la clave, teniendo varias no hay forma de saber cuál abre cuál salvo probando, y un backup que no se sabe abrir no es un backup. Es un SHA-256 truncado con prefijo de dominio: compara claves, no las reconstruye. **No** reetiqueta las 8 copias antiguas: hacerlo exigiría la clave que precisamente no consta | F-047; `EncryptedBackup.key_fingerprint`; `test_la_huella_distingue_claves` | Bajo | Sí | No requerida |

## Reglas

- Toda decisión estructural debe enlazar evidencia.
- Las decisiones sobre identidad, seguridad, autonomía, Qualia, Cristal, migraciones, despliegue o eliminación requieren aprobación humana.
- No borrar decisiones antiguas; añadir una nueva que las reemplace cuando cambie el criterio.
