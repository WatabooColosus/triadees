# Hallazgos del supervisor externo

Registra problemas comprobados, incluso cuando no sean la prioridad actual.

Todos los hallazgos de este ciclo salen de `artifacts/internal_graphs/`, generado
con `python scripts/build_internal_graphs.py`, y de la base viva
`triade/memory/triade.db` abierta en `mode=ro`. Ninguno proviene de documentación.

| ID | Fecha | Área | Hallazgo | Evidencia | Severidad | Estado | PR/Issue |
|---|---|---|---|---|---|---|---|
| F-000 | 2026-08-03 | Auditoría | Auditoría inicial ejecutada por Claude | este documento | Informativo | CLOSED | PR #69 |
| F-001 | 2026-08-03 | Supervisor | El agente exigía `scripts/build_internal_graphs.py` en su arranque obligatorio, y el script no existía ni en esta rama ni en `main`: el supervisor era inejecutable | `.claude/agents/triade-external-supervisor.md:23`; `git cat-file -e main:scripts/build_internal_graphs.py` falla | Alta | CLOSED | PR #69 |
| F-002 | 2026-08-03 | Memoria / Workers | `mission_planner` condicionaba `semantic_memory_governance` a `semantic_memory` (0 filas, tabla retirada) mientras la ingesta escribe en `semantic_documents` (186 filas `candidate`). La tarea nunca se encoló: 0 de 4 777 en `worker_tasks` y 0 de 5 452 en `autonomous_tasks` | `triade/workers/mission_planner.py:350`; consulta ro sobre la base viva | Alta | CLOSED | PR #69 |
| F-003 | 2026-08-03 | Observabilidad | `test_neural_graph_is_read_only` falla en la propia rama de PR #68: en `neuron_activity` el nodo del registro se identifica con `neuron_id`, así que origen y destino coinciden y la arista `uses_neuron` no se emite nunca | `triade/observability/neural_graph.py:113`; pytest sobre `origin/feat/internal-system-graphs` | Media | OPEN | PR #68 |
| F-004 | 2026-08-03 | Bodega / SQLite | 172 tablas se declaran o escriben en código y no existen en la base viva. Parte serán bases distintas y parte esquema muerto: hay que separarlas una a una antes de concluir nada | `artifacts/internal_graphs/table_graph.json` (`live=false`) | Media | OPEN | — |
| F-005 | 2026-08-03 | Qualia / Hipotálamo | **Corregido el 2026-08-03: la mitad de este hallazgo era un fallo de mi instrumento, no del sistema.** Qualia **sí** se lee y **sí** influye: `runner.py:346` recupera señales recientes y `hypothalamus.apply_qualia_signals` modula riesgo, urgencia y tono de cada conversación. Las 4 tablas `qualia_*` aparecían con 0 lectores porque `qualia/store.py` las consulta con `f"SELECT * FROM {table}"` y el extractor no resolvía el nombre interpolado. Queda el residuo verdadero: 7 tablas vivas sin ningún lector, la mayor `metabolic_signals` con 70 293 filas y un único `INSERT` en `metabolism/signals.py:49` | `runner.py:346`; `hypothalamus.py:289`; `table_graph.json` antes y después | Alta | PARTIAL | rama `fix/qualia-readers` |
| F-006 | 2026-08-03 | Educación | `neuron_education_applications`: 0 filas con 6 lectores y 5 escritores. El ciclo educativo no llega a aplicarse ni una vez | `table_graph.json`; base viva | Alta | OPEN | — |
| F-007 | 2026-08-03 | Workers | 9 tipos de tarea tienen handler y 0 ejecuciones: `bodega_global_review`, `federation_inbox_review`, `goal_install`, `goal_lora_train`, `self_improvement_canary_observation`, `self_improvement_evaluation`, `semantic_memory_governance`, `stable_consolidation_review`, `write_governed_text_artifact` | `worker_graph.json`, estado `disconnected` | Alta | PARTIAL | F-002 cierra uno |
| F-008 | 2026-08-03 | Entrypoints | 63 de 77 ficheros con guard `__main__` no los arranca nadie: ni Procfile, ni Dockerfile, ni systemd, ni workflows, ni `[project.scripts]` | `entrypoint_graph.json` | Media | OPEN | — |
| F-009 | 2026-08-03 | Código muerto | 54 módulos bajo `triade/` y `apps/` no tienen ningún importador (403 contando tests y scripts) | `import_graph.json`, estado `disconnected` | Media | OPEN | — |
| F-010 | 2026-08-03 | Código muerto | 4 441 de 6 753 símbolos no tienen ningún llamador demostrable estáticamente. El número es un techo, no una verdad: excluye llamadas dinámicas y descarta los homónimos | `call_graph.json` | Baja | OPEN | — |
| F-011 | 2026-08-03 | Pruebas | `triade/memory/schemas.sql` no declara `learning_evidence` ni `semantic_documents`. Con ese fixture el bloque baseline del planner aborta en la primera consulta y las pruebas ejercitan una ruta truncada sin decirlo | `tests/test_mission_planner.py:make_db`; excepción `no such table: learning_evidence` | Alta | OPEN | — |
| F-012 | 2026-08-03 | Observabilidad | El atlas físico recorría `node_modules`, `.git` y `runs/`: 96 036 nodos, de los cuales 74 665 eran salidas de ejecución. Inservible como mapa del sistema | `file_graph.json` antes del arreglo | Media | CLOSED | PR #69 |
| F-013 | 2026-08-03 | Observabilidad | El extractor de SQL aplicaba `FROM (\w+)` sobre el texto del fichero, así que `from pathlib import Path` producía una tabla llamada `pathlib`: 1 032 tablas falsas frente a 279 reales | `table_graph.json` antes del arreglo | Alta | CLOSED | PR #69 |
| F-014 | 2026-08-03 | Continuidad vital | El eslabón `Bodega` es el único de los once que aparece sin actividad sostenida: `episodic_memory` 241 filas, `semantic_memory` 0. La cadena llega hasta el almacén y ahí se estrecha | `vital_chain_graph.json` | Alta | OPEN | ligado a F-002 |
| F-016 | 2026-08-03 | Workers / Observabilidad | `_system_debt_scan` devolvía una frase fija sin escanear nada, durante 600 ejecuciones (265 en `worker_tasks`, 335 en `autonomous_tasks`), y publicaba esa frase en Qualia como si fuera una observación | `worker_loop.py:_system_debt_scan` antes del arreglo | Alta | CLOSED | PR #69 |
| F-017 | 2026-08-03 | Educación / Gates | Ninguna neurona está vacía: las 25 tienen training, evidencia o actividad (10 `stable`, 6 `experimental`, 6 `candidate_reviewable`, 3 `quarantined`). Lo que está vacío es el umbral: 8 currículos y 8 competencias frente a **0 certificaciones** y **0 aplicaciones**. Nada ha cruzado nunca el gate | consulta ro sobre `neurons`, `neuron_training`, `neuron_evidence`, `neuron_activity`, `neuron_certifications`, `neuron_education_applications` | Alta | OPEN | — |
| F-018 | 2026-08-03 | Aprendizaje | **Resuelto por decisión del operador el 2026-08-03**: los errores y los gates bajos sí generan conocimiento. Implementado por la ruta gobernada que ya existía (`ingest_learning`), con un límite: entra como `candidate`, nunca como evidencia. Sólo `evidence_verified` y `stable` llegan al prompt, así que el saber se acumula hacia el umbral sin volverse doctrina por haber ocurrido una vez | `worker_loop._learn_from_failure`; `tests/test_failures_become_knowledge.py` | Alta | CLOSED | PR #69 |
| F-019 | 2026-08-03 | Observabilidad | El extractor de SQL producía tablas inexistentes: `UPDATE … SET` daba una tabla `set` en 19 módulos, `CREATE TABLE IF NOT EXISTS` partido en líneas daba `if`, y el docstring «Update cognitive load from real sensor data» daba `cognitive`. Corregido exigiendo mayúsculas al SQL y descartando palabras reservadas: 275 tablas en vez de 279 | `table_graph.json` antes y después | Media | CLOSED | PR #69 |
| F-020 | 2026-08-03 | Workers / Cola | **Toda** tarea pasa por `running → completion_uncertain → completed`. La ambigüedad no es excepcional, es el camino normal, así que el estado no distingue una duda real de un trámite | `autonomous_task_transitions`, 68 acciones observadas en 170 s | Media | OPEN | — |
| F-021 | 2026-08-03 | Runtime | El heartbeat informó `workers_active: false` con «revisar autostart/watchdog» durante ~9 min tras el reinicio, mientras `mission_service`, `neuron_nutrition` e `internal_runtime` sí ejecutaban. La bandera describe sólo el bucle de Living Workers, pero el mensaje sugiere que el sistema está parado | `/api/runtime/heartbeat`; `worker_events` 02:42–02:51 | Media | OPEN | — |
| F-022 | 2026-08-03 | Educación / Gates | La brecha del gate no está en la puntuación sino en la competencia. Las neuronas puntúan 0,885–0,903 (`neuron_scores`, 516 filas), pero **las 8 competencias tienen `confidence = 0.0`, `retention_score = 0.0` y `success_count = 0`**. Hubo 26 sesiones de educación y 30 eventos, y ninguna dejó competencia acreditada: por eso nada cruza el umbral | consulta ro sobre `neuron_competencies`, `neuron_scores`, `neuron_education_sessions` | Alta | OPEN | — |
| F-023 | 2026-08-03 | Metabolismo / Necesidades | El metabolismo sólo detecta necesidades de infraestructura: de 13 199 filas en `metabolic_needs` los únicos tipos son `heartbeat` (5 269), `health_check` (5 269), `budget_check` (2 655) y `lease_supervision` (6). No existe ni una necesidad de conocimiento, herramienta, saber, recuerdo, uso o análisis, así que la cadena vital nace ciega a lo cognitivo | `SELECT kind, COUNT(*) FROM metabolic_needs GROUP BY kind` | Alta | OPEN | — |
| F-024 | 2026-08-03 | Observabilidad | El grafo de tablas ignoraba el SQL con nombre de tabla interpolado (`f"SELECT * FROM {table}"`), así que un módulo que crea sus tablas y las lee por un helper genérico aparecía con cero lectores. Produjo la conclusión falsa de F-005. Corregido atribuyendo el acceso dinámico a las tablas que el propio módulo declara | `runtime_graph._flatten_fstring`; `tests/test_internal_graphs.py`; deuda 162 → 158 | Alta | CLOSED | rama `fix/qualia-readers` |
| F-025 | 2026-08-03 | Observabilidad | Los imports relativos dentro de un `__init__.py` se resolvían un nivel por encima: `from .bootstrap import x` en `triade/capabilities/__init__.py` apuntaba a `triade.bootstrap`, inexistente. **10 módulos vivos aparecían como código muerto** | `code_graph._absolute_module`; huérfanos 54 → 44 | Alta | CLOSED | rama `fix/qualia-readers` |
| F-026 | 2026-08-03 | Observabilidad | Un entrypoint sin lanzador automático se contaba como código muerto aunque la documentación explicara cómo ejecutarlo. **45 de 62 eran herramientas manuales documentadas**, no deuda. Ahora se separan: 45 `legacy`, 17 huérfanas de verdad | `code_graph._documented_paths`; `entrypoint_graph.json` | Alta | CLOSED | rama `fix/qualia-readers` |
| F-027 | 2026-08-03 | Supervisión | **La deuda medida bajó de 162 a 103 elementos sin tocar el runtime.** Los 59 que desaparecieron eran defectos de medición del propio grafo (F-024, F-025, F-026), no mejoras del sistema. Queda escrito para que nadie lea esa bajada como progreso | `introspection.build_debt_report` antes y después | Informativo | CLOSED | rama `fix/qualia-readers` |
| F-028 | 2026-08-03 | Observabilidad | Un `__init__.py` se contaba como huérfano si nadie lo nombraba, pero Python lo ejecuta al importar cualquier módulo del paquete. Marcaba como muerto el `__init__` de paquetes en uso, incluido el de `triade/observability` | `code_graph.build_import_graph`; huérfanos 44 → 31 | Media | CLOSED | rama `fix/qualia-readers` |
| F-029 | 2026-08-03 | Sandbox / Seguridad | El sandbox vivo es `triade/sandbox/executor.py` (`run_in_sandbox`). `secure_executor.py` (252 líneas) y `secure_executor_v2.py` (442 líneas) **no los importa nadie**, y `tool_registry.py` está superado por `enhanced_tool_registry.py`. La constitución declara `secure_executor` y `tool_registry` como capacidades por nombre: los nombres del contrato no coinciden con la implementación viva. `sandbox_executions` tiene 0 filas | `constitution/enforcer.py:115`; `sandbox/__init__.py:10`; `import_graph.json` | Alta | OPEN | — |
| F-030 | 2026-08-03 | Bodega / Duplicación | Dos escritores para la misma tabla: `core/bodega.py:319` escribe `episodic_memory` y es el vivo; `memory/episodic_memory.py` (345 líneas) tiene su propio INSERT sobre la misma tabla más `episodic_memory_history`, y no lo importa nadie | `import_graph.json`; `grep 'INTO episodic_memory'` | Media | OPEN | — |
| F-031 | 2026-08-03 | Código muerto | 31 módulos de producción sin ningún importador, **8 077 líneas**. Borrados el 2026-08-03 con autorización del operador, bajo sus dos condiciones: backup previo y verificación de que nada era útil. Backup en `artifacts/dead_code_backup/orphan-modules-20260803T040159Z.tar.gz` con SHA-256 por fichero. Verificación: sin `importlib` dinámico en el repo, grep estricto de imports absolutos y relativos sobre 1 108 ficheros más configs y systemd, ningún test los importa, y tras borrar el paquete entero sigue importando con 0 fallos | `artifacts/dead_code_backup/orphan-modules-20260803T040159Z.json`; suite completa | Media | CLOSED | rama `fix/qualia-readers` |
| F-032 | 2026-08-03 | Verificación | La primera comprobación dio **3 falsos positivos** —`federation.py`, `canary.py`, `rollback.py` parecían importados—. Los imports reales eran `from .federation import`, `from .canary import` y `from .rollback import` apuntando a **ficheros homónimos de otros paquetes** que sí existen. Confiar en el grep sin abrir las líneas habría dejado módulos muertos o borrado los vivos | `triade/federation/federation.py`, `triade/self_improvement/canary.py`, `triade/regression/rollback.py` | Informativo | CLOSED | rama `fix/qualia-readers` |
| F-015 | 2026-08-03 | Federación | `federated_merge_nodes` y `metabolic_config` no tienen lectores ni escritores en el código, y están vacías en la base | `table_graph.json` | Baja | OPEN | — |
| F-033 | 2026-08-03 | Observabilidad | El panel de deuda presentaba como medición actual artefactos de hace 2 h. La estructura sale de `artifacts/internal_graphs/` con `allow_build=False` y una caducidad de 6 h, y **en el Studio nada los regeneraba**: el único disparador automático era `.github/workflows/internal-graphs.yml`, sobre `pull_request`. Tras borrar los 31 módulos de F-031 a las 04:05, el panel siguió contando 31 módulos huérfanos y 90 elementos hasta las 05:56. La cifra real al regenerar era **56** | `artifacts/internal_graphs/index.json` con `mtime` 03:52 frente al commit `93496c8` de 04:05; `apps/routes/ui.py:123`; `introspection.py:34` | Alta | CLOSED | rama `fix/observability-graphs-autorefresh` |
| F-034 | 2026-08-03 | Métrica | `debt_items_total` **baja al borrar un escritor**. `tables_with_writer_and_no_rows` exige que exista un escritor, así que `benchmark_results`, `benchmark_tasks` y `federated_merge_log` salieron del recuento al quedarse con cero aristas, sin haber ganado una sola fila: salieron por degradación, no por arreglo. No existe categoría para "tabla que nadie escribe ni lee" —son 10 de 235, 9 vacías— y por tanto la métrica premia borrar el escritor | `table_graph.json` (0 aristas para las tres); `introspection.py:149-152` | Media | OPEN | — |
| F-035 | 2026-08-03 | Cola / Coherencia | `completion_uncertain` con `reason=artifact_publication_pending` **no es un fallo**: es la primera mitad de un commit en dos fases que resuelve a `completed` en milisegundos. 5 565 de 5 565 resolvieron así; 0 tareas quedan en ese estado. Pero el marcador se escribe en `last_error` y **ahí se queda**: 5 566 tareas `completed` llevan `last_error='artifact_publication_pending'`. `goal_orchestrator.py:231` lo expone como `error`, así que una tarea correcta se lee como fallida. Borrarlo sin más no vale: `task_leases.py:660` documenta "nunca borra `last_error`" porque de eso depende la recuperación de `retry_wait` | `SELECT last_error,COUNT(*) FROM autonomous_tasks WHERE status='completed'` → 5 566; transiciones 11730→11731 (4 ms); `task_leases.py:650-661` | Media | OPEN | — |
| F-036 | 2026-08-03 | Observabilidad | El registro de acciones pinta como `unknown` las 5 565 mitades de commit rutinarias, porque `completion_uncertain` está en `_AMBIGUOUS_STATUSES`. Las genuinamente inciertas son otras 19 (`recovery_no_artifact`, todas terminadas en `dead_letter`) y quedan indistinguibles del ruido. Es la misma dilución contra la que avisa el comentario de `event_feed.py:219`: "si casi todo cae en unknown, el registro se vuelve gris y un error real pasa desapercibido" | `event_feed.py:219-240`; recuento por `reason` sobre `autonomous_task_transitions` | Baja | OPEN | — |
| F-037 | 2026-08-03 | Aprendizaje | **Livelock en la selección de evidencia.** `mission_planner.py:328` filtra por `source_type='experience'`, y de los 665 candidatos `internally_checked` sólo **1** lo cumple (`tool` 262, `conversation` 194, `qualia_bus` 176, `web` 32, `experience` 1). Ese único elegible, `exp-9abb4978bf5b4b59`, no es medible: `build_probe` devuelve `None`, el handler sale con `no_op` **sin escribir fila en `learning_evidence`**, el `NOT EXISTS` de la consulta sigue siendo cierto y el planner vuelve a elegirlo. Las 400 tareas más recientes llevan el mismo `candidate_id` y los 465 eventos registrados dicen todos `sin_prueba_objetiva`. Consume un hueco de tarea cada ~2 min. Mientras tanto **420 de los 665 sí son medibles** | `SELECT source_type,COUNT(*) FROM learning_queue WHERE status='internally_checked'`; `build_probe` sobre los 665; `worker_events` filtrados por `task_type` | Alta | OPEN | — |
| F-038 | 2026-08-03 | Aprendizaje | Consecuencia de F-037: `learning_evidence` tiene 8 filas (2 `improved`, ambas de candidatos `exp-*`), así que `evidence_bridge.require_improvement()` lanza siempre. Y como `pipeline.mark_used_in_run` actualiza el contador de usos **antes** del gate de evidencia, hay candidatos con **44 usos y `avg_outcome_score` 0.934 congelados en `internally_checked`**: 16 cumplen `MIN_RUN_USES=3` y `MIN_OUTCOME_SCORE=0.70` y ninguno promociona. `validated_in_runs`=0 → `stable_consolidation_review` nunca se planifica → nada llega a memoria estable | `pipeline.py:412-421`; `evidence_bridge.py:185-191`; `learning_queue` cruzada con `learning_evidence` | Alta | OPEN | — |
| F-039 | 2026-08-03 | Automejora | `improvement_proposals` e `improvement_canaries` **no existen como tablas** en la base viva: `store.py:44` y `canary.py:23` las crean con `CREATE TABLE IF NOT EXISTS`, y nunca se ejecutó. Los ocho módulos de `triade/self_improvement/` están importados y activos en el grafo, pero `create_proposal` sólo lo llaman los tests. Por eso `_plan_self_improvement` y `_plan_canary_observation` salen por el guard de `sqlite_master` y `self_improvement_evaluation` / `self_improvement_canary_observation` tienen 0 ejecuciones | `sqlite_master` sin ambas tablas; `import_graph.json` (8 módulos `active`); `grep create_proposal` → sólo `tests/` | Media | OPEN | — |
| F-040 | 2026-08-03 | Despliegue / Vigilancia | **3 de las 5 unidades declaradas en `deploy/systemd/` no tienen proceso vivo**: `triade-watchdog` (`scripts/runtime_watchdog.py`), `triade-backup` (`scripts/runtime_backup.py`) y `triade-workers` (`scripts/runtime_workers.py`). Los workers sí funcionan, pero como hilo del proceso de la API (`continuous_runner`, 19 hilos en el PID 17942), no como el servicio declarado. El watchdog y el backup no corren de ninguna forma: `runtime_health_snapshots` lleva 3,1 d congelada y `backup_restore_drills` 4,2 d. El código está inervado —`triade/runtime/watchdog.py` lo importan 5 módulos— y el grafo lo ve; lo que no había era categoría que lo contara | `deploy/systemd/*.service` frente a `/proc/*/cmdline`; `runtime_health_snapshots` MAX(created_at) | Alta | OPEN | — |
| F-041 | 2026-08-03 | Observabilidad | El estado `legacy` del grafo de entrypoints —creado para no llamar deuda a 45 utilidades manuales— dejaba fuera del recuento a los órganos que sí deberían estar corriendo: `runtime_watchdog.py` y `triade_doctor_full.py` salían `legacy` y por tanto invisibles. Corregido con la categoría `declared_services_not_running`, que distingue por quién declaró que algo debía arrancarse (un `.service`) y no por si alguien lo citó en un `.md` | `code_graph.py:447`; `introspection._declared_services_not_running` | Media | CLOSED | rama `fix/debt-repair-and-introspection` |
| F-042 | 2026-08-03 | Planificación | `learning_evidence` no está en `schemas.sql`: la crea `LearningEvidenceBridge` la primera vez que se usa. En una instalación nueva la tabla no existe, y el `sqlite3.Error` de la consulta que la referencia **abortaba todo el bloque `_plan_baseline`** —dedupe, gobernanza semántica, autopromoción, evidencia—, no sólo esa rama. Sólo sobrevivía `pulse_check`. Detectado al escribir la prueba de F-037 sobre una base recién creada | `query_internal_errors` → `[mission_planner.baseline] no such table: learning_evidence`; `tests/test_evidence_selection_livelock.py` | Alta | CLOSED | rama `fix/debt-repair-and-introspection` |
| F-043 | 2026-08-03 | Metabolismo | El metabolismo **mide pero no regula el camino que gasta**. `metabolic_signals` tiene un único `INSERT` (`metabolism/signals.py:49`) y **cero lectores**; no existe ningún fichero `homeosta*`. Los 12 módulos de `triade/metabolism/` están activos, pero sólo los importan `apps/routes/api.py` y su propio coordinador: `worker_loop.py` no menciona `resource_governor`, `autonomy_budget` ni el metabolismo en ninguna línea. Su único freno es `check_ollama_blood()`, que comprueba si el modelo responde, no cuánto se está gastando. El coordinador arranca desde `single_port_app.py:126` y lo consultan rutas HTTP —es decir, un humano preguntando— no el consumidor de recursos | `grep metabolic_signals` → 1 escritor, 0 lectores; `grep -n resource_governor triade/workers/worker_loop.py` → sin coincidencias; `import_graph.json` | Alta | OPEN | — |

## Reglas

- No registrar datos simulados.
- Diferenciar evidencia actual de evidencia histórica.
- No cerrar un hallazgo sin prueba o ejecución posterior.
- Mantener referencias a archivo, símbolo, tabla, run, log, prueba o artefacto.
