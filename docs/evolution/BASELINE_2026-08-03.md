# Baseline evolutiva — 2026-08-03

## Identidad de la ejecución

- SHA de `main` local, `origin/main` y remoto antes de modificar: `09b0248038cd4a2689355610cf823683a8a869e2`.
- Rama de captura: `phase/01-learning-end-to-end`, creada desde `phase/evolution-program-v1`.
- Fecha UTC: `2026-08-03`.
- Python: `3.12.11`.
- SO: Ubuntu/Linux `6.8.0-1064-gcp`, x86_64.
- Dependencias: 222 distribuciones; `pip freeze` SHA-256 `86a2688f9421777afd8365f8df5e89371b766bb86eb505ac411345d18e1499d8`.

## Gates observados antes de modificar

| Gate | Resultado | Duración | CPU | RSS máximo informado |
|---|---:|---:|---:|---:|
| `compileall` | pasa | 1,42 s | 25 % | 13 KiB* |
| `ruff check .` | falla: 659 | 0,23 s | 324 % | 30 KiB* |
| `ruff format --check .` | falla: 6 archivos | 0,05 s | 175 % | 18 KiB* |
| `mypy triade` | falla: 26 errores/4 archivos | 8,17 s | 95 % | 458 KiB* |
| `pytest -q` | bloqueado sin resultado terminal; interrumpido | 380,70 s en copia aislada | 13 % | ~199 MiB observado por `ps` |

\* El `time` integrado de zsh etiqueta `%M` como RSS, pero sus valores no son coherentes con `ps`; se conservan como salida instrumental, no como capacidad demostrada.

La colección previa al cambio contiene 2.106 pruebas, consistente con el estado inicial declarado. Se hicieron dos intentos: el primero quedó contaminado cuando el árbol de trabajo cambió y fue descartado; el segundo se ejecutó desde una exportación aislada del SHA base. Este segundo intento avanzó más allá del 3 %, quedó esperando en `threading.py`/socket sin resultado nuevo y fue interrumpido tras 380,70 s. No hubo fallo mostrado antes de la espera, pero tampoco existe resultado terminal; por tanto no se convierte el dato inicial declarado de “2.106 completas” en ejecución demostrada.

## Recursos y runtime observados

- RAM del host: 33.651.986.432 bytes; disponible durante la muestra: 26.055.503.872 bytes.
- SQLite productivo observado: `triade/memory/triade.db`, 188.665.856 bytes.
- Todos los SQLite encontrados, incluyendo caches, backups y runs históricos: 3.862.663.168 bytes en la primera muestra. No todos son runtime productivo.
- Runtime: proceso `uvicorn apps.single_port_app:app` vivo en puerto 8010, ~1,32 GiB RSS y ~175 % CPU en la muestra.
- Heartbeat: `GET /api/runtime/heartbeat` no respondió en 30,001 s (`curl` exit 28, HTTP 000). Es runtime observado, no un endpoint sano demostrado.
- GPU: NVIDIA L4, 23.034 MiB; utilización instantánea 0 %.
- Ollama: `qwen2.5:3b-instruct` y `nomic-embed-text` cargados, ambos declarados `100% GPU`; ~3.346 MiB de GPU ocupada.

## Interpretación

- Código existente: pipeline, evidencia, regresión, retrieval y consolidación.
- Código alcanzable: imports y suites específicas alcanzan esas rutas.
- Código probado: las suites históricas cubren piezas, no la misión completa de Fase 1.
- Código ejecutado: gates arriba y observaciones de runtime.
- Runtime observado: servidor vivo, Ollama en GPU y heartbeat bloqueado >30 s.
- Capacidad demostrada en baseline: ninguna mejora aprendida end-to-end; falta intervención comparada.

Esta baseline no oculta deuda: los fallos previos de Ruff y mypy permanecen abiertos y bloquean el gate global aun si la Fase 1 no introduce regresiones nuevas.
