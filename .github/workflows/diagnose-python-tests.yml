name: Diagnose Python Tests

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  diagnose:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/learning-evidence-bridge
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Capture first failure or hanging test
        id: pytest
        shell: bash
        continue-on-error: true
        run: |
          set +e
          timeout 10m pytest -vv --maxfail=1 > diagnostic_failure.txt 2>&1
          code=$?
          echo "exit_code=$code" >> diagnostic_failure.txt
          exit $code
      - name: Trim diagnostic
        if: steps.pytest.outcome == 'failure'
        run: tail -160 diagnostic_failure.txt > diagnostic_failure.trimmed && mv diagnostic_failure.trimmed diagnostic_failure.txt
      - name: Commit diagnostic
        if: steps.pytest.outcome == 'failure'
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add diagnostic_failure.txt
          git commit -m "ci: capture failing or hanging Python test"
          git push origin HEAD:feat/learning-evidence-bridge
      - name: Fail when pytest failed or timed out
        if: steps.pytest.outcome == 'failure'
        run: exit 1
