plugins {
    id "com.android.application"
}

android {
    namespace "local.triade.node"
    compileSdk 35

    defaultConfig {
        applicationId "local.triade.node"
        minSdk 26
        targetSdk 35
        versionCode 1
        versionName "0.4.0"
    }
}
